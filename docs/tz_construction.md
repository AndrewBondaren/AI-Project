# ТЗ: Система строительства (Construction)

> **Статус:** placeholder. Система не проектировалась детально.

## Scope

Механика возведения объектов в мире: дороги, здания, мосты, укрепления и другие физические структуры.

**Terrain под стройкой / раскопкой** — **локальный patch** в `patch_bounds`, не regen мира. Generate: `TerrainPatchGeneratorService` ([tz_terrain_generation.md](./tz_terrain_generation.md) § Локальная модификация); persist: `MapCellService.persist_terrain_patch`; orchestration: DAG `excavate` / `modify_terrain`.

## Ключевые концепции

- **Мастерство строительства** — навык персонажа / фракции, влияющий на качество возводимого объекта. Чем выше мастерство, тем лучше финальные параметры: `road_tier_durability`, прочность здания, скорость строительства.
- **Материалы** — расходуются при строительстве; тип и количество зависят от шаблона объекта.
- **Время** — строительство занимает игровое время; прогресс хранится в `under_construction` на объекте.
- **Transition `under_construction → done`** — момент завершения; объект становится активным в мире.

## Связи с другими ТЗ

- [tz_structure_connections.md](tz_structure_connections.md) — `under_construction` / `under_repair` на дорогах; `road_tier_durability` зависит от мастерства строителя
- [tz_building_generator.md](tz_building_generator.md) — здания; quality-параметры зданий

## Открытые вопросы

| Вопрос | Статус |
|---|---|
| Модель мастерства строительства — навык персонажа, фракции или обоих? | не описан |
| Формула влияния мастерства на quality-параметры | не описан |
| Механика прогресса строительства во времени | не описан |
| Ремонт (`under_repair`) — отдельная механика или ветка construction? | не описан |
| Разрушение объектов — связь с construction? | не описан |
