"""Wire season keys — climate / world calendar."""

from __future__ import annotations

from enum import StrEnum


class SeasonKey(StrEnum):
    WINTER = "winter"
    SPRING = "spring"
    SUMMER = "summer"
    AUTUMN = "autumn"
